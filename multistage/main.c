#include <stdio.h>
//print app description
int main() {
   printf("BMI Calculator that saves name, height, weight, bmi and date for given input. Height is displayed in cm, weight in kg. BMI is fixed to 2 decimals");
   return 0;
}